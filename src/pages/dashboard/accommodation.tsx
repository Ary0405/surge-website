import { Layout } from "~/components/layout";
import { api } from "~/utils/api";

const Accommodation = () => {
    return (
        <div>
            <h1>Accommodation</h1>
        </div>
    )
}

export default Accommodation